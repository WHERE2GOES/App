name: data
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  dio: ^5.8.0+1
  freezed_annotation: ^3.0.0
  flutter_dotenv: ^5.2.1
  http: ^1.4.0
  get_it: ^8.0.3
  injectable: ^2.5.0
  json_annotation: ^4.9.0
  logger: ^2.6.0
  retrofit: ^4.6.0
  shared_preferences: ^2.5.3

  openapi:
    git:
      url: https://github.com/WHERE2GOES/Dart-Client
      ref: main

dev_dependencies:
  flutter_lints: ^5.0.0

  build_runner: ^2.4.15
  freezed: ^3.0.6
  injectable_generator: ^2.7.0
  json_serializable: ^6.9.5
  retrofit_generator: ^9.6.0
