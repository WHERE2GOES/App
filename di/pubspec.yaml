name: di
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  data:
    path: ../data
  home:
    path: ../feature/home
  login:
    path: ../feature/login
  navigation:
    path: ../feature/navigation
  reward:
    path: ../feature/reward
  mypage:
    path: ../feature/mypage
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  assets:
    - ../.env