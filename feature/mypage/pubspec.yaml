name: mypage
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../../core
  design:
    path: ../../design

  get_it: ^8.0.3

dev_dependencies:
  flutter_lints: ^5.0.0
